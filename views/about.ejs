<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>
  <body>
    <h1>This is about page</h1>

    <% if(blogs.length > 0){ %> <% blogs.forEach(blog =>{ %>
    <h3><%= blog.title %></h3>
    <p><%= blog.snippet %></p>
    <p><%= blog.body %></p>
    <%})%> <%}%>
  </body>
</html>
